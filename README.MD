## Hubcrag Flipping Card

### Frontend dependencies


### Setup demo project

`npm install` or `yarn install`

### Start demo project

`gulp` or `gulp serve` deploy app and start browsersync server + watchers

## File structure
```bash
├── /html/                     # Html version
└── /pug/                      # Pug version
